// examples/demo.ltl — runnable demo for ltl_dns_resolver

import std::io
use ltl_dns_resolver as lib

fn main() {
    io::println("ltl_dns_resolver demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}