// ltl-dns-resolver/main.ltl
//
// Recursive DNS resolver — root hints to final answer.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-dns-resolver v0.1.0")
    io::println("Recursive DNS resolver — root hints to final answer.")
    0
}
